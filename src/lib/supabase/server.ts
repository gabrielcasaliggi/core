/**
 * Re-export del cliente Supabase para uso en API routes (server-side).
 * Centraliza la importación en un path semántico: @/lib/supabase/server
 */
export { createServerClient } from "./client";
